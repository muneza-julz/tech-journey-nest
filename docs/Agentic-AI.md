1. terminal-based, not IDE;
2. Works with all tools';
3. Fits into all existing workflows';
4. General purpose - you can use it for almost anything;
5. Infinitelly hackable;

# Set-up: 
    # Webpack: packaging tool ( bundles all data files into a single readable code for the browser)

    # ts.loader: translater, for translating the typescript code into javascript

    # tsc: typescript compiler converts the translated code into vanilla js 

Prompts: https://x.com/i/status/2060473758721446365
## Your codebase:
- How is @RoutingController.py used?
- How do I make a new @app/services/ValidationTemplateFactory?
- Why does recoverFromException take so many arguments? Look through git history to answer
- Why did we fix issue #18363 by adding the if/else in @src/login.ts API
- In which version did we release the now @api/ext/PreHooks.php API?
- Look at Pr #9383, then carefully verify which app versions were impacted what did I ship last week?

## Your workflow: - with Claude Code
-Use Claude Code to answer questions about your codebase
-Use codebase Q&A as a way to dip your feet into Claude Code ( don't start by using fancy tools nor editing codes ...)
 
# Plugin your team's tools ( bash ', MCP ', etc.)

# Common workflows
1.  do a little bit of exploration, planning, confirmation before diving into coding, commit
→   Figure out the root cause for issue #983, then propose a few fixes. Let me choose an approach before you code, ultrathink

2. Write tests(uni or integration) > commit > code > iterate > commit
→   Give it a sort of tool that gives it feedback, by itself it eventually gives perfect outcome after iteration

→   Write tests for @utils/markdown.ts to make links render properly (note the tests won't pass yet. Since links aren't yet implemented). Then commit. then update teh code to  make the tests pass

3. Write code > screenshot result > iterate
→   Implement (mock.png).then screenshot it with Puppeteer and iterate till it looks like the mock.

# 3# teach Claude to use your tools
# 4# figure out the right workflow for the task

./<enterprise root>/CLAUDE.md → shared across all projects
.~/.claude/CLAUDE.md → shared across all projects
.project-root/
    CLAUDE.md           → checked in
    CLAUDE.local.md     → not checked in
u47\77















































































































































































































































































































































































































































